{% extends "Tom32iSiteBundle::layout.html.twig" %}

{% set success = app.session.flashbag.get('success') %}

{% block content %}

	{% if success %}
	<div class="alert alert-success alert-block">
		<button type="button" class="close" data-dismiss="alert">&times;</button>
		<h4>Félicitations !</h4>
		{% for s in success %}
			<p>{{ s }}</p>
		{% endfor %}
	</div>
	{% endif %}

	<form class="form-horizontal" action="" method="post" {{ form_enctype(form) }}>
	    
	    <div class="control-group">{{ form_widget(form.content, {attr: {placeholder: "Mon secret..."}}) }}</div>

	    {{ form_rest(form) }}

	  <div class="control-group"><button type="submit" class="btn btn-danger">Envoyer</button></div>
	</form>

{% endblock %}